spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost/data_processing
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
